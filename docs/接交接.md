# xhs-note-publish 接交接（交接文档）

> 交接日期：2026-08-21 · 生成方式：归档整理 skill「交接准备」步骤自动产出
> 续接方式：说**「按交接提示继续」** → 读本文件 + `xhs-note-publish-resume-handoff` 记忆，对齐锚点后无缝继续

---

## 一、项目现状（✅ 已归档，无未提交改动）

| 维度 | 值 |
|---|---|
| 项目 | xhs-note-publish — 笔记/宝贝发布工具（小红书 + 闲鱼） |
| 版本 | **v1.3.0**（最新 tag `archive-20260821-v3`） |
| 本地 | `E:\myClaudCodeWorkspace\xhs-note-publish` |
| Git | `https://github.com/wxx591139000/xhs-note-publish.git` · 分支 `master` |
| 公网 | `https://xhs.zhuanlu.xyz`（本地 :8800，经本地 cloudflared 隧道 `xhs-tunnel`） |
| v1.3.0 补 | `docs/API使用手册.md`：开放 HTTP 写入，其他项目可写推广文案→自动落待发布草稿（小红书+闲鱼双平台，端到端实测） |
| v1.3.0 | 桌面「笔记队列」按用途/平台筛选（所有/📕小红书/🐟闲鱼/🔀通用，可与状态筛选叠加）+ 修复手机「🐟复制宝贝文案」拼入内部 purpose 字段 |
| v1.2.0 | 闲鱼宝贝信息 + 手机「🐟复制宝贝文案」+ 用途标记（小红书/闲鱼/通用） |

## 二、数据 & 迁移说明

- 数据库：`data/notes.db`；上传图片：`data/uploads/`；备份复制这两者即可
- v1.2.0 新增 `notes.meta` 列（JSON），旧库启动即自动 `ALTER` 补齐，无需手动迁移
- 存量 15 篇笔记已标 `purpose=xhs`（小红书）

## 三、启动方式（脚本在本仓库）

| 脚本 | 作用 |
|---|---|
| `start.bat` | 同时拉起服务(python app.py) + 隧道 xhs-tunnel（两个新窗口） |
| `run.bat` | 仅本机起服务（不拉隧道） |

> 🌉 隧道必须用 `xhs-tunnel`(27da88b4)，**严禁用旧 transcribe-bot 隧道重启本地 cloudflared**（抢 VPS 隧道致 upload.zhuanlu.xyz 502）。正确启动：`cd C:\Users\Dancing\.cloudflared && cloudflared tunnel run xhs-tunnel`

> ⚠️ **改模板须重启服务**：`templates/*.html` 是 Flask 模板（内存缓存），改了要杀 `python app.py` 新进程再拉起才生效；`static/*`（样式/JS）则是静态文件，改完刷新即可，不必重启。

## 四、遗留待办（非阻塞）

- 闲鱼 AutoX.js 自动填充（当前乱走手动复制，如需自动填闲鱼编辑器另做脚本）
- 桌面端预览卡片未展示宝贝信息（不影响发布链路）

## 五、硬约束（务必遵守）

1. 🔑 key/Token 读 `manager/.env` 或环境变量，不硬编码（本仓库无敏感 key，改时可参考转录 bot）
2. 📄 文档改动必升版本号
3. ⏰ 判断时间用 `date`
4. 🖼️ 不直接预览图片（会卡死）；改图从 HTML/文本源处理
5. 🌐 现代模型链纯文本，收到图/视频先转文本
6. 🔧 改飞书/API/隧道配置前必须先确认
7. GitHub 上传/下载走 `github-upload` skill；push 只在用户要求时
8. 🌉 隧道双约定（上）；cloudflared 配置见 `COORDINATION.md`