# 归档摘要 archive-20260821-v2 — v1.3.0 桌面队列按用途筛选

> 归档日期：2026-08-21 · 版本：**v1.3.0** · tag：`archive-20260821-v2` · 分支：master

## 本次变更摘要

- **新增：桌面「笔记队列」按用途/平台筛选（v1.3.0）**
  - 右侧「笔记队列」标题下新增一行分类 chips：**所有 / 📕小红书 / 🐟闲鱼 / 🔀通用**
  - 点「所有」= 列出全部类型笔记（默认）；点单一分类 = 只显示对应 `meta.purpose` 的笔记
  - 与原有**状态筛选**（全部/待发布/草稿/已发布）可**叠加**使用（如「闲鱼 + 待发布」）
  - 改动：`templates/desktop.html`（新增 `.purpose-filter` 行 + 静态资源版本号升 `-3`）、`static/app.js`（`currentPurposeFilter` + `purposeOf()` + `[data-pfilter]` 事件）、`static/style.css`（`.list-head` 纵向布局、筛选项可换行）
- **修复：手机「🐟复制宝贝文案」多拼内部 purpose 字段**
  - `static/mobile.js` `copyItem()` 原来把 `meta` 所有键值当宝贝字段拼文案，导致粘贴后结尾多一行 `purpose:idlefish`；现过滤 `k === 'purpose'`，只拼 标题 + 正文 + 实际宝贝字段
- **本次含模板改动**，已重启服务（旧 PID 32912 → 新 PID 34596）使模板重载，端到端验证 `/app` 返回新分类行

## 5 份核心文档（均升 v1.3.0）

- [SPEC.md](SPEC.md) — 功能表加「队列按用途筛选」，用途标记行补桌面筛选
- [ARCHITECTURE.md](ARCHITECTURE.md) — 关键设计决策加第 6 条「桌面/手机筛选用途一致 + 独立选择器」
- [TEST_PLAN.md](TEST_PLAN.md) — 加「桌面队列按用途筛选 + 复制宝贝文案」测试段（v1.3.0）
- [PITFALLS.md](PITFALLS.md) — 加 3 条坑：复制宝贝文案拼 purpose、Flask 模板内存缓存需重启、筛选容器类复用误绑
- [PROGRESS.md](PROGRESS.md) — 里程碑 v1.3.0 + 已完成功能补两条

## Git 统计

- 本次改动 4 个源文件（static/app.js · static/mobile.js · static/style.css · templates/desktop.html）+ 文档套件 + CHANGELOG + README
- 见 `git log archive-20260821..archive-20260821-v2`

## 剩余待办

- 闲鱼 AutoX.js 自动填充（当前走手动复制，如需自动填闲鱼编辑器另做脚本）→[忽略]
- 桌面端预览卡片尚未展示宝贝信息（不影响发布链路）