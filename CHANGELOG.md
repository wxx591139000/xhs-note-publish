# 更新日志

## [v1.3.0] - 2026-08-21 - 桌面队列按用途筛选 + 复制宝贝文案修复

### 新增
- 桌面「笔记队列」新增**按用途/平台分类筛选**：所有 / 📕小红书 / 🐟闲鱼 / 🔀通用，点击直接显示对应分类；点「所有」列出全部类型
- 用途筛选可与原有**状态筛选**（全部/待发布/草稿/已发布）叠加使用
- `templates/desktop.html` 队列头部改纵向布局，塞下两行筛选；`static/style.css` 允许筛选项换行

### 修复
- 手机端「🐟 复制宝贝文案」把内部 `meta.purpose`（用途标记）也当宝贝字段拼进文案，导致粘贴后原文结尾多一行 `purpose:idlefish`；现复制时排除 `purpose`，只拼标题+正文+实际宝贝字段

### 说明
- 本次为**模板改动**，Flask 对模板有内存缓存，需**重启服务**才生效（静态文件无需）
- 新增 `docs/API使用手册.md`：开放 HTTP 接口，其他项目/程序可写推广文案→自动存「待发布」草稿（小红书+闲鱼双平台，端到端实测），含三步速通与可直接调用示例

## [v1.2.0] - 2026-08-21 - 闲鱼宝贝支持 + 用途标记

### 新增
- 笔记支持**闲鱼平台发布**（手动模式 + 手机「🐟 复制宝贝文案」）
- 每篇可选「闲鱼宝贝信息」：价格/成色/发货地/运费等（`键:值` 多行，存 `notes.meta` JSON 列）
- 手机端新增「🐟 复制宝贝文案」按钮：一键拼出 标题+正文+宝贝字段 的闲鱼发布文案
- **用途标记**：每篇标 📕小红书 / 🐟闲鱼 / 🔀通用；桌面徽标、手机端平台筛选、按钮按用途收敛
- 历史笔记未标记时视为「通用」，两平台按钮都显示

### 说明
- 沿用「辅助发布」理念：闲鱼同样不做 API 自动发布，人工粘贴最稳
- 数据迁移：`notes` 表新增 `meta TEXT DEFAULT '{}'` 列，旧库启动时 `ALTER` 自动补，不破坏数据

## [v1.1.2] - 2026-08-18 - 隧道配置修正

### 修复
- `start.bat` 的隧道命令从旧的 `transcribe-bot` 改为正确的 `xhs-tunnel`（`27da88b4`），并 `cd` 到 `~/.cloudflared` 加载 config
- **原隐患**：用 `transcribe-bot` 隧道会抢 VPS 转录 bot 的隧道，导致 `upload.zhuanlu.xyz` 等域名 502
- 对齐 `COORDINATION.md` 双隧道约定：本地小红书只走 `xhs-tunnel`，与 VPS 的 `transcribe-bot` 彻底隔离

## [v1.1.1] - 2026-08-14 - 复制标题

### 新增
- 手机端笔记卡片新增「✏️ 复制标题」按钮，单独复制标题（小红书标题独立输入框）
- 卡片按钮布局改自动换行，容纳 4 个按钮

## [v1.1.0] - 2026-08-14 - 图片体验优化

### 新增
- 手机端图片「逐张保存」：每张用户亲手点「保存此图」，绕开浏览器批量下载拦截
- 点图放大层：封面/缩略图点击全屏，长按保存兜底
- 静态资源防缓存版本号（`?v=`），解决手机浏览器缓存旧 JS

### 修复
- 手机端批量下载被拦截/弹窗闪烁 → 改逐张确认流程
- 改代码后手机端「没变化」→ 静态资源带版本号

### 说明
- 明确 cloudflare 双隧道协调：本地 xhs-tunnel 只管 xhs，VPS 管转录三域名

## [v1.0.0] - 2026-08-14 - 归档版

### 新增
- 小红书笔记发布工具首版：电脑端素材管理 + 手机端一键复制发布
- 密码保护、二维码访问、队列状态机（pending/drafted/published）
- 产线 md 稿子一键/批量导入（含标题前缀匹配图片）
- AutoX.js 自动填充（存草稿模式）+ 自动化免登录凭据接口
- cloudflare 双隧道协调（本地 xhs-tunnel 只管 xhs，VPS 管转录三域名）
- 首次生成完整文档套件（docs/）

### 修复
- 产线导入：标签抓取、正文 H1 剔除、图片 `|尺寸` 后缀、标题前缀匹配图片
- 误用旧隧道抢 VPS 的隧道协调问题（已拆双隧道）

### 说明
- 项目目前非 Git 仓库，后续如需版本管理可 `git init`